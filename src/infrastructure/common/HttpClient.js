export default class HttpClient {
    
    async sendAsync(req) {
        // return new Promise((resolve, reject) => {
        //     request(req, (error, response, body) => {
        //         if (error)
        //             reject(error)

        //         resolve(response);
        //     });
        // });
        return null;
    }
}